version: "3.9"

services:
  postgres:
    image: postgres:latest
    container_name: rocketpay-postgres
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=rocketpay_dev
    ports:
      - "5432:5432"

